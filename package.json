{
  "name": "@ground-truth/getcache-onlinemap",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mapbox online map. A CHILD: takes its host as props and stands alone without one.",
  "exports": {
    "./lib/*.svelte": "./lib/*.svelte",
    "./lib/*.css": "./lib/*.css",
    "./lib/*.js": "./lib/*.ts",
    "./lib/*": "./lib/*.ts",
    "./routes/*.svelte": "./routes/*.svelte",
    "./routes/*.js": "./routes/*.ts",
    "./routes/*": "./routes/*.ts",
    "./hooks": "./hooks.ts",
    "./deps.json": "./deps.json",
    "./package.json": "./package.json"
  },
  "dependencies": {
    "@capacitor/core": "^8.3.0",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/share": "^8.0.1",
    "@tmcw/togeojson": "^7.1.2",
    "@turf/turf": "^7.2.0",
    "fflate": "^0.8.2",
    "geojson": "^0.5.0",
    "mapbox-gl": "^3.24.0",
    "maplibre-gl": "^5.24.0",
    "svelte-sonner": "^1.0.6"
  },
  "peerDependencies": {
    "svelte": "^5.19.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "scripts": {
    "test": "vitest run"
  },
  "_no_build_on_purpose": "A child ships no vite.config.ts. It is not an app; it is a folder a parent builds. That is why the escape/case guard is a VITE PLUGIN in each parent (unskippable: no build, no deploy) and a VITEST TEST here (portable: runs in a bare clone with no app around it). Same rule, two shapes, because a plugin needs a build to hook into and a child has none.",
  "_why_pinned": "Versions match what ReTreever runs, because that is the version the code was written and tested against. They were all \"*\" until 25 Aug 2026, which is not a constraint but a wish: the workspace install resolved pbf to 5.1.2, which dropped the default export, and the build died on `import Pbf from \"pbf\"`. Nested inside a parent the child silently borrowed the parent's version and the hole never showed. A child that is genuinely portable has to state what it needs.",
  "_why_exports": "Subpath patterns, because a package's exports get NO extension resolution: `./lib/*` -> `./lib/*` would map lib/coord to a literal file that does not exist. Longest key wins, so `*.svelte` beats `*`; `*.js` -> `*.ts` serves TS-style ESM imports. Every module a parent imports goes through this map — add a folder here or it is unreachable by package name."
}
